## Summary

<!-- What does this PR change, and why? -->

## Test plan

- [ ] `./gradlew ktlintCheck detekt lint` passes
- [ ] `./gradlew testDebugUnitTest` passes
- [ ] `./gradlew connectedDebugAndroidTest` passes (if UI/DB behavior changed)
- [ ] Manually verified on device/emulator (if UI-facing)

## Screenshots (if UI-facing)

<!-- Before / after -->
